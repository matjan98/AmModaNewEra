# Production build uses relative API paths (server/api/...) when VITE_API_BASE is unset.
# Do not put localhost here — it gets baked into quasar build and breaks deployed sites.

# Local development only (.env.development):
# VITE_API_BASE=http://localhost:8080

# Public site origin for index.html (canonical, og:url, og:image). No trailing slash.
# Used at build time by quasar.config.js (defaults to https://ammoda.pl).
# SITE_ORIGIN=https://staging.example.com
